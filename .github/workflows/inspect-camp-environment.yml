name: Inspect camp environment renderer

on:
  push:
    branches:
      - agent/refactor-collision-architecture
    paths:
      - scripts/extract-camp-environment-snippet.mjs
      - .github/workflows/inspect-camp-environment.yml

permissions:
  contents: write

jobs:
  inspect:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/refactor-collision-architecture
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/extract-camp-environment-snippet.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/camp/_environment-snippet.txt
          git commit -m "chore: snapshot camp environment renderer" || exit 0
          git push origin HEAD:agent/refactor-collision-architecture
